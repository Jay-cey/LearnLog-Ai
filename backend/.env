# Database Configuration (Localhost)
# Format: postgresql+asyncpg://<username>:<password>@<host>:<port>/<database_name>
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/learnlog

# Security
SECRET_KEY=dev_secret_key_change_this_for_production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Optional: OpenAI API Key for AI features (if not using local mocks)
# OPENAI_API_KEY=sk-...
